import { Button } from '@/components/ui/button';

export default function Downloads(){
    return (
        <section >
            <h1>Downloads</h1>
            <div className='flex flex-wrap gap-2 justify-around'>
                <Button>
                    Icone de download | icon  Curriculo || TCC || Artigo
                </Button>
                <Button>
                    Icone de download | icon  Curriculo || TCC || Artigo
                </Button>
                <Button>
                    Icone de download | icon  Curriculo || TCC || Artigo
                </Button>
                <Button>
                    Icone de download | icon  Curriculo || TCC || Artigo
                </Button>
                <Button>
                    Icone de download | icon  Curriculo || TCC || Artigo
                </Button>
                <Button>
                    Icone de download | icon  Curriculo || TCC || Artigo
                </Button>
            </div>
        </section>
    )
}